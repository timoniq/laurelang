!use "@/array".

: ?inc(int) -> int,
  ?flatten('T'[][]) -> 'T'[].

?inc(x) -> x + 1.

?flatten([]) -> [].
?flatten([x]) -> x.

?flatten(array) -> flattened {
    linked(head, tail) = array;
    flattened = head:append(tail:flatten());
}