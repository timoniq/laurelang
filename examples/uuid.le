: ?person(string, natural) -> ID.
?person("Anya", 21).
?person("Nick", 19).
?person("John", 43).

: ?predicate(person, person).
?predicate(p1, person(name, _)) {
    p1 = person(_, age);
    name; age;
}
