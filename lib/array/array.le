: ?cle_include("array.so").

: ?each_int(int[]) -> int.
?each_int(arr) -> element when each(arr) = element.

: ?no_repeat(int[]).

?no_repeat([]).
?no_repeat(arr) when length of arr = 1.

?no_repeat(arr) {
    i > 0;
    i < length of arr;
    &all i {
        arr[i] cons arr[i - 1];
    };
}